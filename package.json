{
  "name": "helios-3d-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=helios-3d-site",
    "build": "npm run build --workspace=helios-3d-site",
    "preview": "npm run preview --workspace=helios-3d-site",
    "test": "npm run test --workspace=helios-3d-site",
    "lint": "npm run lint --workspace=helios-3d-site"
  }
}
